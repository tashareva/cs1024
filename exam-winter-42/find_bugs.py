def find_bugs(task: list, correct_task: list) -> list:
    task = []
    correct_task = [A -> B, B->C, C->+]
    for task: 
        if task == correct_task:
            print('No problems')
        elif task = [A->B, B->C, C->B]:
            task = [A -> B, B->C, C->+]
        elif task = [A->A, B->C, C->A]:
            print('More than 1 mistake')
        elif task = [A->A, B->B]:
            print('More than 1 mistake')
    return task 
